<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.imageMapper">
    <insert id="insertImageInfo" parameterType="map" >
        begin;
        insert into image_map.contents(x, y, title, description, user_id, image_id) values(#{x}, #{y}, #{title}, #{description}, #{user_id}, #{image_id});
        insert into image_map.image(path, name, user_id) values (#{path}, #{name}, ${user_id});
        commit;
    </insert>
</mapper>